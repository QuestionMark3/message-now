<% if unread === 0 %>
	<% disp = 'none' %>
<% else %>
	<% disp = 'block' %>
<% end %>
<!-- Chatroom -->
<div class="chatroom card" data-chatroom_id="<%= chatroom.id %>">
<div unselectable="on" class="content">
	<i class="tv icon"></i>
	<div class="floating ui red label" id="chatroom_<%= chatroom.id %>_badge" style="display:<%= disp %>"><%= unread %></div>
	<b><%= chatroom.title %></b>
</div>
</div>
<!-- /Chatroom -->